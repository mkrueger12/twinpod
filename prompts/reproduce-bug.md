---
agent: build-agent
---
Read {{reads}} for Linear issue {{issue_id}}.

Reproduce the bug as a failing regression test before changing behavior. Write the reproduction summary, test command, and failing evidence to {{writes}}.
