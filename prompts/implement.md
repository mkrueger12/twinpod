Read {{reads}} for Linear issue {{issue_id}} in worktree {{worktree_path}}.

Implement the requested change. Add or update tests at the feature boundary. Run the appropriate suite and keep iterating until it is green. If the issue is blocked by ambiguity or broken infrastructure, stop and explain the andon condition.
