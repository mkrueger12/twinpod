---
agent: create-research-questions-agent
---
Use the create-research-questions skill for Linear issue {{issue_id}}: {{issue_title}}.

The ticket is at {{reads}}. Read it fully, then produce the research-questions document under `.twinpod/{{issue_id}}-<slug>/` per the skill's directory convention. This is an unattended run — there is no human to consult, so make your own judgment calls instead of pausing.
