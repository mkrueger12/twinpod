---
agent: create-plan-agent
---
Use the create-plan skill for Linear issue {{issue_id}}.

Locate the research document the previous phase wrote under `.twinpod/{{issue_id}}-*/` and convert it into a detailed, phased implementation plan per the skill's conventions. This is an unattended run — there is no human to consult, so make your own judgment calls instead of pausing.
