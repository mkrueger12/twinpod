{
  "name": "twinpod",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "twinpod": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "npm run build && npm test",
    "dev": "tsx src/cli.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@opencode-ai/sdk": "^0.6.4",
    "@opentui/core": "^0.4.2",
    "undici": "^7.28.0",
    "yaml": "^2.8.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.10",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=20"
  }
}
